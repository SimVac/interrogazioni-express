<!DOCTYPE html>
<html lang="en">

<head>
  <%- include('../partials/head', {title: "Home"}) %>
</head>

<body>
  <div class="hero min-h-screen bg-base-200">
    <div class="hero-content text-center">
      <div class="max-w-md">
        <div role="alert" class="alert">
          <svg xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" class="stroke-info shrink-0 w-6 h-6">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13 16h-1v-4h-1m1-4h.01M21 12a9 9 0 11-18 0 9 9 0 0118 0z"></path>
          </svg>
          <span class="text-l">Portate i diplomi mancanti l'8 gennaio</span>
        </div>
        <br>

        <h1 class="text-3xl font-bold">Scegli la materia</h1>
        <br>
        <br>
        <div class="grid grid-cols-2 gap-4 place-content-center">
          <% for (materia of materie) { %>
          <%if (materia.attivo) {%>
            <button class="btn btn-primary m-0.5" onclick="location.href = '/materia/<%= materia.endpoint %>'"><%= materia.abbreviazione %></button>
          <% } %>
          <% } %>
        </div>
      </div>
    </div>
  </div>
  <%- include('../partials/navbar') %>
</body>

</html>